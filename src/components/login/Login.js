import React from 'react';
import Nav from '../nav/Nav';

const Login = () => {
      return (
            <div>
                  <Nav/>
                  Login
            </div>
      );
};

export default Login;