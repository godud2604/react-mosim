import React from 'react';
import Nav from '../nav/Nav';

const Profile = () => {
      return (
            <div>
                  <Nav/>
                  PROFILE
            </div>
      );
};

export default Profile;