import React from 'react';
import Nav from '../nav/Nav';

const Client = () => {
      return (
            <div>
                  <Nav/>
                  Client
            </div>
      );
};

export default Client;